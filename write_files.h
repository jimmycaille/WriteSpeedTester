#ifndef WF_HEADER_FILE
#define WF_HEADER_FILE

static int writeFiles(int,int,char*,char*,int);
static double diff(struct timespec,struct timespec);
static int createFolder(char*,char*);
static int cleanFolder(char*);

#endif